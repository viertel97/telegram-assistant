name: $(Date:yyyyMMdd)-$(Build.BuildId)

trigger:
- main
- master

resources:
- repo: self

variables:
- group: common

extends:
  template: pipeline-template.yaml
  parameters:
      repository: "telegram-assistant"